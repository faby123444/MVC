@model IEnumerable<ConexionAppWeb_Apigateway.Models.DB.Recompensa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Título)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Detalle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Título)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Detalle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRecompensa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRecompensa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRecompensa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
